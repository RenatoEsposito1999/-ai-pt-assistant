✅ Flusso completo del caso d’uso "Registrazione"
1️⃣ L’utente clicca il bottone nella View
La View cattura l’evento ma non contiene logica.
Il bottone ha un listener legato a un metodo nel Controller.
2️⃣ Controller
Raccoglie i dati dal form (nome, email, password…).
Richiama il metodo nel Service, passando questi dati.
3️⃣ Service
Applica la logica applicativa (es. verifica che l’email non esista già).
Istanzia il Model (es. PersonalTrainer).
Chiama il DAO per salvare nel database.
4️⃣ DAO
Esegue la vera operazione SQL (INSERT nel DB).
Restituisce al Service l’esito.
🔁 SE VIENE TUTTO OK:
Il Service restituisce un “OK” al Controller.
Il Controller aggiorna la View (es. “registrazione completata”).


Quindi creo il model, creo il dao che è la connessione al db e eventuali funzioni che fanno contorlli a livello di db, vado nel service e do questo dao, nel service c'è la logica di business dell'app
il service dunque è chiamato dal controller dell'entità. 

# Compila
javac --module-path $PATH_TO_FX --add-modules javafx.controls,javafx.fxml -cp "lib/*" -d out src/**/*.java

# Esegui
java --module-path $PATH_TO_FX --add-modules javafx.controls,javafx.fxml -cp "out:lib/*" src.MainApp
